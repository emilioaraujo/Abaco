<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android">

    <group android:checkableBehavior="single">
        <item
            android:id="@+id/nav_home"
            android:icon="@android:drawable/ic_dialog_dialer"
            android:title="DashBoard" />
        <item
            android:id="@+id/nav_accounts"
            android:icon="@drawable/ic_menu_send"
            android:title="Accounts" />
        <item
            android:id="@+id/nav_income_categories"
            android:icon="@drawable/ic_menu_send"
            android:title="Income Categories" />
        <item
            android:id="@+id/nav_expense_categories"
            android:icon="@drawable/ic_menu_send"
            android:title="Expense Categories" />
        <item
            android:id="@+id/nav_persons_entities"
            android:icon="@drawable/ic_menu_send"
            android:title="Persons - Entities" />
    </group>

    <item android:title="Application">
        <menu>

            <item
                android:id="@+id/nav_settings"
                android:icon="@drawable/ic_menu_manage"
                android:title="Settings" />
            <item
                android:id="@+id/nav_about"
                android:icon="@drawable/ic_menu_send"
                android:title="About" />
        </menu>
    </item>

</menu>
